#############################################
# Environment Example (DO NOT COMMIT REAL SECRETS)
# Copy to .env.local for local dev
#############################################

# Database (Neon Postgres)
DATABASE_URL="postgresql://USER:PASSWORD@HOST/dbname?sslmode=require"

# Optional default provider API keys (users can override via UI; leave blank in prod if preferred)
GEMINI_API_KEY=""
KIMI_API_KEY=""
DEEPSEEK_API_KEY=""

# Vercel Blob RW token (optional for saving prompt JSON)
BLOB_READ_WRITE_TOKEN=""

# Encryption secret used for: HMAC signing session cookie + API key encryption key derivation seed
# Use a long random string (>48 chars). Example generate: `openssl rand -base64 48`
ENCRYPTION_SECRET="CHANGE_ME_TO_LONG_RANDOM_BASE64"

# Notes:
# - Never commit a real DATABASE_URL or provider keys.
# - If you omit provider keys here, users must supply theirs in the UI.
# - Rotate ENCRYPTION_SECRET carefully; existing encrypted API keys will become unreadable.
